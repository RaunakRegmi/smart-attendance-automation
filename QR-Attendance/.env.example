NODE_ENV=development
PORT=4000

# Change in production
JWT_SECRET=dev_jwt_secret_change_me
JWT_EXPIRES_IN=1d

# Postgres connection
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=qr_attendance

# Used to compute PRESENT vs LATE threshold (minutes after scheduled start)
LATE_GRACE_MINUTES=0
